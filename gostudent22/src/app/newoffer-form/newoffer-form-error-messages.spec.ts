import { NewofferFormErrorMessages } from './newoffer-form-error-messages';

describe('NewofferFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new NewofferFormErrorMessages()).toBeTruthy();
  });
});
